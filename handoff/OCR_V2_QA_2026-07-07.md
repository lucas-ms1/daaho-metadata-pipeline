# OCR-v2 QA Note, 2026-07-07

> **SUPERSEDED HISTORICAL JULY NOTE.** The August 24 corrected release is
> authoritative; `out/` and OCR-v2 are rollback/experimental evidence, not the
> current descriptive release. The five record decisions are complete. Use
> `SUCCESSOR_GUIDE_2026-08-28.md` and `QA_FLAG_DISPOSITION_2026-08-28.md` for
> current status. The exact blank-transcript cause was not conclusively
> reproduced; current tests verify envelope/tier preservation and explicit
> transcript assignment, not a definitive reconstruction of that cause.

## Status Verdict

- `out/` remains the final stable baseline.
- `out_ocr_v2_gpt54mini/` remains experimental and should not replace `out/` wholesale.
- OCR-v2 may be useful case-by-case for transcript or evidence comparison, but not as a metadata replacement without manual correction.

## Validation Warning Inventory

Source report: `out/validation_report_ocr_v2_current_audit.csv`

| File | Code | OCR-v2 place | Baseline place | Evidence line | Assessment |
|---|---|---|---|---|---|
| `BC-0692_Recto.loc15.json` | `place_mismatch_with_header_sender` | `Ohio--Oxford` | `Ohio--Oxford` | `from Oxford, Ohio`, parsed as `Ohio--from Oxford` | Likely validator false positive; OCR-v2 and baseline agree on place. |
| `BC-0710_Recto.loc15.json` | `place_mismatch_with_header_sender` | `Ohio--Oxford` | `Massachusetts--Glouchester` | `Gloucaster, Massachusetts`, parsed as `Massachusetts--Gloucaster` | OCR-v2 place is worse than baseline; needs human review before any correction. |

## Row-Level Notes

- `BC-0692`: likely validator false positive caused by parsing `from Oxford, Ohio` as `Ohio--from Oxford`; OCR-v2 and baseline both use `Ohio--Oxford`.
- `BC-0710`: OCR-v2 place is worse. OCR-v2 uses `Ohio--Oxford`; baseline uses `Massachusetts--Glouchester`; `vocab/fast_places.txt` contains the approved spelling `Massachusetts--Gloucester`, so any future correction should use that approved spelling after human review.

## Successor Instructions

- Do not adopt OCR-v2 wholesale.
- If revisiting OCR-v2, start with `BC-0710`.
- Keep `out/` and `handoff/freeze_2026-07-07_out_baseline/` as read-only evidence.
- Make any future OCR-v2 corrections in a new output folder or clearly versioned copy.
