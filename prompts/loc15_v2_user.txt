FILENAME: {filename}

Use strict MAP-oriented cataloging for this item.

SCHEMA-FIRST OUTPUT:
- Populate only schema fields.
- Populate required keys.
- If evidence is missing, use null/empty as allowed by schema.
- "decade" must always be null (pipeline derives it).

TITLE + DATE CONTRACT (strict):
1) title must be non-empty and sentence-capitalized.
2) date must be ISO 8601 (YYYY / YYYY-MM / YYYY-MM-DD) or null.
3) Title must end with ", {{DATE_SUFFIX}}" derived from date:
   - YYYY-MM-DD -> D Month YYYY (no comma in suffix)
   - YYYY-MM -> Month YYYY
   - YYYY -> YYYY
4) If date is null, title must end with ", undated".
5) "undated" may appear only in title suffix, nowhere else.
6) Do not output ISO dates at end of title (for example ", 1937-10-19" is invalid).
7) For YYYY-MM-DD dates, year is mandatory in the title suffix.
   Invalid: "..., 16 November"
   Valid: "..., 16 November 1937"

LOCATION CONTRACT (strict):
1) place must use FAST-style State--City tokens.
2) Prioritize sender/creation location.
3) If both sender and recipient locations are present, output both with semicolon:
   SenderState--SenderCity; RecipientState--RecipientCity
4) Do not output City, State formatting.

EXAMPLES:
- Letter to Mr. Kiyoshi Tomizawa from the President, 19 October 1937
- Itinerary document for Upham, undated
- District of Columbia--Washington
- Pennsylvania--Philadelphia; Ohio--Oxford

OCR TEXT:
{ocr_text}

HINTS (defaults; copy verbatim only if non-empty and schema has field):
default.repository={known_repository}
default.collection={known_collection}
default.series=
default.folder=
default.box=
default.identifier=
default.call_number=
default.digital_identifier=
default.reproduction_number=
default.permalink={known_permalink}
default.digital_collection=
default.digital_publisher=
default.digitized=
