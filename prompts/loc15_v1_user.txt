FILENAME: {filename}

STRICT CATALOGING RULES - Follow the Asian Americans at Miami Mini MAP guidelines exactly:

SCHEMA-FIRST OUTPUT:
- Populate only fields that exist in the schema.
- Populate every schema-required field in a way that satisfies the schema constraints.
- If evidence is missing, use the most conservative allowed fallback (e.g., devise a descriptive title; use ISO YYYY if only a year is visible; use 'undated' only if the schema permits it).

OPTIONAL BUT RECOMMENDED FIELDS:
- description: Concise 1-2 sentence summary addressing who, what, where, why
- creator: Last name, first name format (or direct order for institutions)
- contributors: If present in schema, output as a JSON array of strings (no semicolons)
- correspondents: If present in schema, output as a JSON array of strings (no semicolons)
- publisher: Direct order as it appears
- place: FAST format [state/country]--[city] (e.g., 'Ohio--Oxford')
- language: Language of the resource
- keywords: Short topical keywords as a JSON array
- decade: YYYY-YYYY if date is known, otherwise null/empty
- repository: Repository name (Tier 3 default only)

SUBJECT FIELD MAPPING:
- subjects: MUST be a JSON array of individual strings, NOT a semicolon-separated string
  - Each FAST subject term should be a separate array element
  - Example: ['Student movements', 'Flags', 'Floats (Parades)'] NOT ['Student movements; Flags']
- If the schema provides separate local/people subject fields, use them as JSON arrays of strings; otherwise keep subjects in the main subjects array.
Controlled vocabulary guidance: use FAST/AAT-like controlled terms; keep them short; avoid punctuation/subfields; prefer generic controlled terms over interpretation.

CRITICAL FIELD DIFFERENCES:
- 'transcript': Cleanest verbatim transcription with special markers:
  - Use [HANDWRITTEN: ...] to clearly mark any handwritten portions
  - Use [ILLEGIBLE] or [UNCLEAR] for unreadable parts
  - Preserve line breaks and original formatting
- 'text_reading': Linear, simplified reading text for computational analysis:
  - Clean, flowing text without special markers
  - Combine typed and handwritten into continuous text
  - Remove formatting, preserve only content
- 'title' vs 'generated_title': Use 'title' for the cataloged title per MAP guidelines.
  'generated_title' can be used if different from the formal title.

TIER POLICY:
- Tier 1 fields (safe to auto-generate): transcript, text_reading, description, title, generated_title, date, names (people), places, keywords, decade.
- Tier 2 fields (AI-proposed): subjects, genre, creator, publisher, theme. Be conservative.
- Tier 3 fields (do NOT generate): rights, repository, collection, series/folder/box hierarchy, identifiers, call numbers, digital identifiers, reproduction numbers, permalinks, digital collection/publisher, digitized flag.
- Tier 3 rule: Output null/empty unless a default hint is explicitly provided for that exact field; if provided, copy it verbatim; never infer.

VALIDATION RULES:
- Use on-item evidence. Prefer labelled data (e.g., 'Author:', 'Date:').
- For any list field in the schema (e.g., subjects/genre), output JSON arrays of individual strings.
- field_confidence: 0-100 integers for each populated field; 0 if null/empty.
- Dates: ISO 8601 (YYYY, YYYY-MM, YYYY-MM-DD). Do not output ranges/decades/circa strings.

TIER 3 DEFAULTS (copy verbatim if schema has these fields; otherwise ignore):
- If a default.* value below is non-empty, set the schema field to that exact string/boolean; else null/empty.
- Never populate rights unless it is explicitly provided as a default (otherwise null/empty).

OCR TEXT:
{ocr_text}

HINTS (defaults; if blank, ignore):
default.repository={known_repository}
default.collection={known_collection}
default.series=
default.folder=
default.box=
default.identifier=
default.call_number=
default.digital_identifier=
default.reproduction_number=
default.permalink={known_permalink}
default.digital_collection=
default.digital_publisher=
default.digitized=